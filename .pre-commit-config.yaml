repos:
  - repo: local
    hooks:
      - id: prettier
        name: Format source files
        entry: npx --no-install prettier --write --ignore-unknown
        language: system
        exclude: ^(AGENTS\.md|plan/README\.md|package-lock\.json)$
